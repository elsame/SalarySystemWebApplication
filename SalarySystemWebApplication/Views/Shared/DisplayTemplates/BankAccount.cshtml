@model String
@{
    /**/
    <span>
        @String.Format("{0}-{1}-{2}",
                              Model.Substring(0, 4),
                              Model.Substring(4, 2),
                              Model.Substring(6, 6))
    </span>
}
